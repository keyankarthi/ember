
import { oops } from 'superrentel/helpers/oops';
import { module, test } from 'qunit';

module('Unit | Helper | oops');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = oops([42]);
  assert.ok(result);
});

